diff --git a/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListener.java b/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListener.java
index 350e4c2759057b37f649f7b0f82f81f14bc96155..205a8b5700f459ae2957bd231ab377a16627dc99 100644
--- a/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListener.java
+++ b/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListener.java
@@ -36,7 +36,12 @@ public class RNAndroidNotificationListener extends NotificationListenerService {
 
         HeadlessJsTaskService.acquireWakeLockNow(context);
 
-        context.startService(serviceIntent);
+        try {
+            Log.d(TAG, "Starting headless task for notification from " + sbn.getPackageName());
+            context.startService(serviceIntent);
+        } catch (Exception e) {
+            Log.e(TAG, "Failed to start notification headless task", e);
+        }
     }
 
     @Override
diff --git a/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListenerHeadlessJsTaskService.java b/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListenerHeadlessJsTaskService.java
index 17ed906c2a7adb7568cd4a9e9248f478f1e6a10e..046e4426b12b9da6f9d27fa339d03a6716aace9a 100644
--- a/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListenerHeadlessJsTaskService.java
+++ b/android/src/main/java/com/lesimoes/androidnotificationlistener/RNAndroidNotificationListenerHeadlessJsTaskService.java
@@ -2,16 +2,20 @@ package com.lesimoes.androidnotificationlistener;
 
 import android.content.Intent;
 import android.os.Bundle;
+import android.util.Log;
 import com.facebook.react.HeadlessJsTaskService;
 import com.facebook.react.bridge.Arguments;
 import com.facebook.react.jstasks.HeadlessJsTaskConfig;
 import javax.annotation.Nullable;
 
 public class RNAndroidNotificationListenerHeadlessJsTaskService extends HeadlessJsTaskService {
+    private static final String TAG = "RNAndroidNotificationListener";
+
     @Override
     protected @Nullable HeadlessJsTaskConfig getTaskConfig(Intent intent) {
-        Bundle extras = intent.getExtras();
+        Bundle extras = intent != null ? intent.getExtras() : null;
         if (extras != null) {
+            Log.d(TAG, "Starting JS task RNAndroidNotificationListenerHeadlessJs");
             return new HeadlessJsTaskConfig(
                     "RNAndroidNotificationListenerHeadlessJs",
                     Arguments.fromBundle(extras),
@@ -19,6 +23,7 @@ public class RNAndroidNotificationListenerHeadlessJsTaskService extends Headless
                     true // optional: defines whether or not  the task is allowed in foreground. Default is false
             );
         }
+        Log.w(TAG, "Ignored headless task start: extras are null");
         return null;
     }
 }
